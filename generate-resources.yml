name: Generate and Sync Resources

on:
  push:
    branches:
      - main
    paths:
      - 'resources/**'
      - 'scripts/**'

# 提升权限级别
permissions:
  contents: write
  pull-requests: write

jobs:
  generate:
    runs-on: ubuntu-latest
    # 如果多个任务同时触发，取消之前的，只跑最新的，减少冲突
    concurrency:
      group: ${{ github.workflow }}-${{ github.ref }}
      cancel-in-progress: true

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          # 使用默认 token 即可，但显式声明
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Run generation script
        run: npm run gen
        env:
          # 即使脚本暂未用到，也建议注入以防后续逻辑需要
          API_KEY: ${{ secrets.API_KEY }}

      - name: Auto-commit changes
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "chore: auto-sync resources data [skip ci]"
          file_pattern: 'data/resources.ts resources/*.md'
          commit_user_name: github-actions[bot]
          commit_user_email: github-actions[bot]@users.noreply.github.com
          # 关键设置：如果检测到远程有新提交，尝试集成
          push_options: '--force'
          skip_dirty_check: false    
          skip_fetch: false          
